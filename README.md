# Enhancing-Network-Security-with-Regex-based-IP-Filtering
Regex IP Filter is a Streamlit app that blocks private IPv4/IPv6 and link-local IPv6 addresses using regex rules, while allowing all other IPs.
